{
  "_comment": "Register the gate.cat veto as a Claude Code PreToolUse hook. After `pip install gate.cat` the `gatecat-hook` console script is on PATH — no absolute paths, no python interpreter to guess. Copy the PreToolUse block into your .claude/settings.json.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "gatecat-hook"
          }
        ]
      }
    ]
  }
}
