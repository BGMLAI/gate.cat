{
  "name": "gatecat",
  "description": "Deterministic, fail-closed action veto — vetoes an agent's irreversible shell commands (rm -rf, DROP TABLE, terraform destroy, secret exfil) before they run. Enforcement runs in the harness via a PreToolUse hook, outside the model's control flow. Requires the free core: pip install gate.cat",
  "version": "0.4.18",
  "author": {
    "name": "BGML",
    "email": "bogumil@bgml.ai",
    "url": "https://gate.cat"
  },
  "homepage": "https://gate.cat",
  "repository": "https://github.com/BGMLAI/gate.cat",
  "license": "Apache-2.0"
}
